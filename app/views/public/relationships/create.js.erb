$("#followers").html("<%= @user.followers.count %>")
$("#follow-btn").html("<%= j(link_to "フォローを外す", user_relationships_path(@user.name), method: :delete, remote: true, class: "btn btn-secondary btn-sm d-block mx-auto", id: "unfollow") %>")
